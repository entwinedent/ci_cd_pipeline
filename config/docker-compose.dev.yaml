version: '3.8'

services:
  # Local container registry for development
  registry:
    image: registry:2
    ports:
      - "5000:5000"
    volumes:
      - registry-data:/var/lib/registry
    networks:
      - ci-cd-network

volumes:
  registry-data:

networks:
  ci-cd-network:
    driver: bridge
