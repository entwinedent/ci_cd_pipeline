apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: pod-kill-api-gateway
  namespace: default
spec:
  action: pod-kill
  mode: one
  selector:
    labelSelectors:
      app: go-api-gateway
  duration: "10s"
