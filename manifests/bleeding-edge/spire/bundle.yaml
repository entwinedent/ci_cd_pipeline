apiVersion: v1
kind: ConfigMap
metadata:
  name: spire-bundle
  namespace: spire
data:
  bundle.pem: |
    # This bundle will be generated by SPIRE server on first run
    # The actual X.509 certificate will be created during SPIRE initialization
    # This is a placeholder for the trust bundle
