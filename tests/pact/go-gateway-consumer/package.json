{
  "name": "go-gateway-pact-consumer",
  "version": "1.0.0",
  "description": "Pact consumer tests for Go API Gateway",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "publish": "pact-broker publish ./pacts --consumer-app-version 1.0.0"
  },
  "devDependencies": {
    "@pact-foundation/pact": "^13.0.0",
    "@types/jest": "^29.5.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.3.0"
  }
}
