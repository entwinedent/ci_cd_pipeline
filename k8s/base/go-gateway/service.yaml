apiVersion: v1
kind: Service
metadata:
  name: go-api-gateway
  labels:
    app: go-api-gateway
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: go-api-gateway
